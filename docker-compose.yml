services:
  docs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./docs:/docs/docs
      - ./zensical.toml:/docs/zensical.toml
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
